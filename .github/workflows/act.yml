name: COVID Box

on:
  schedule:
    - cron: '0 */5 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - uses: Saika-Z/covid-box@master
        env:
          GH_PAT: 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
          COUNTRY: ${{ secrets.COUNTRY }}
          GIST_ID: 51180172b81924cbb5d5b2d53ee9ec4d
